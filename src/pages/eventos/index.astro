---
export const prerender = false;

import MainLayout from '../../layouts/MainLayout.astro';
import { query, queryOne } from '../../db/connection';

let upcomingEvents: any[] = [];
let pastEvents: any[] = [];
let carouselBg: string = '';

try {
  upcomingEvents = await query("SELECT * FROM events WHERE status IN ('upcoming', 'active') ORDER BY start_date ASC") as any[];
  pastEvents = await query("SELECT * FROM events WHERE status = 'completed' ORDER BY start_date DESC") as any[];
  const bgSetting = await queryOne("SELECT setting_value FROM site_settings WHERE setting_key = 'events_carousel_bg'") as any;
  carouselBg = bgSetting?.setting_value || '';
} catch (e) {
  console.error('Error loading events:', e);
}

function formatDate(date: any) {
  if (!date) return '';
  const d = new Date(date);
  return d.toLocaleDateString('es-ES', { year: 'numeric', month: 'long', day: 'numeric' });
}

function formatDateEn(date: any) {
  if (!date) return '';
  const d = new Date(date);
  return d.toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' });
}
---

<MainLayout title="FEMUNDO - Eventos" description="Calendario completo de eventos, torneos y campeonatos mundiales de dominó organizados por FEMUNDO.">
  <Fragment slot="head">
    <style>
        .hero-eventos {
            background:
                radial-gradient(ellipse at 15% 50%, rgba(255,140,0,0.18) 0%, transparent 55%),
                radial-gradient(ellipse at 85% 20%, rgba(59,130,246,0.25) 0%, transparent 50%),
                radial-gradient(ellipse at 60% 90%, rgba(255,140,0,0.10) 0%, transparent 45%),
                linear-gradient(135deg, #0a1628 0%, #0f2060 35%, #1e3a8a 65%, #1a3580 100%);
            color: white;
            padding: 80px 20px;
            text-align: center;
            min-height: 45vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .hero-eventos::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 50% 0%, rgba(255,255,255,0.04) 0%, transparent 60%);
            pointer-events: none;
        }

        .hero-eventos::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            height: 3px;
            background: linear-gradient(90deg, transparent, #ff8c00, transparent);
        }

        .hero-eventos .hero-content {
            position: relative;
            z-index: 2;
            max-width: 700px;
        }

        .hero-eventos .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: rgba(255,140,0,0.2);
            border: 1px solid rgba(255,140,0,0.4);
            color: #ffb347;
            padding: 6px 18px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1px;
            margin-bottom: 1.25rem;
        }

        .hero-eventos .hero-content h1 {
            font-size: clamp(2rem, 5vw, 3.8rem);
            font-weight: 800;
            margin-bottom: 1rem;
            line-height: 1.15;
            color: white;
            letter-spacing: -0.5px;
        }

        .hero-eventos .hero-content p {
            font-size: clamp(1rem, 2.5vw, 1.2rem);
            opacity: 0.85;
            max-width: 560px;
            margin: 0 auto;
            line-height: 1.7;
        }

        .eventos-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .eventos-section-title {
            font-family: 'Raleway', sans-serif;
            font-size: clamp(1.4rem, 3vw, 1.9rem);
            font-weight: 800;
            color: var(--primary-dark);
            margin-bottom: 25px;
            display: flex;
            align-items: center;
            gap: 14px;
            padding-bottom: 14px;
            border-bottom: 2px solid #f1f5f9;
            position: relative;
        }

        .eventos-section-title::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 0;
            width: 60px;
            height: 2px;
            background: var(--secondary);
        }

        .eventos-section-title i {
            color: var(--secondary);
            background: rgba(255,140,0,0.1);
            width: 40px;
            height: 40px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1rem;
            flex-shrink: 0;
        }

        .eventos-grid {
            display: flex;
            flex-direction: column;
            gap: 20px;
            margin-bottom: 50px;
        }

        .evento-card {
            background: white;
            border-radius: 18px;
            box-shadow: 0 4px 20px rgba(30, 58, 138, 0.09);
            overflow: hidden;
            transition: all 0.35s cubic-bezier(.22,.68,0,1.1);
            display: flex;
            flex-direction: row;
            border: 1px solid rgba(30, 58, 138, 0.07);
            text-decoration: none;
            color: inherit;
        }

        .evento-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 50px rgba(30, 58, 138, 0.18);
            border-color: rgba(255, 140, 0, 0.25);
        }

        .evento-card-image-wrap {
            flex: 0 0 280px;
            width: 280px;
            overflow: hidden;
            background: linear-gradient(135deg, #1e3a8a, #0f2060);
            position: relative;
            min-height: 200px;
        }

        .evento-card-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
            transition: transform 0.5s ease;
        }

        .evento-card:hover .evento-card-image {
            transform: scale(1.06);
        }

        .evento-card-no-image {
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 200px;
        }

        .evento-card-no-image i {
            font-size: 3rem;
            color: rgba(255,255,255,0.25);
        }

        .evento-card-body {
            padding: 24px 30px;
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: center;
            gap: 6px;
        }

        @media (max-width: 640px) {
            .evento-card {
                flex-direction: column;
            }
            .evento-card-image-wrap {
                flex: 0 0 220px;
                width: 100%;
                height: 220px;
            }
        }

        .evento-status {
            display: inline-block;
            padding: 4px 12px;
            border-radius: 15px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            margin-bottom: 10px;
        }

        .status-upcoming {
            background: #dbeafe;
            color: #1e40af;
        }

        .status-active {
            background: #dcfce7;
            color: #166534;
        }

        .status-completed {
            background: #f1f5f9;
            color: #475569;
        }

        .evento-card-title {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--primary-dark);
            margin-bottom: 10px;
            font-family: 'Raleway', sans-serif;
        }

        .evento-card-title a {
            color: inherit;
            text-decoration: none;
        }

        .evento-card-title a:hover {
            color: var(--secondary);
        }

        .evento-meta {
            display: flex;
            flex-direction: column;
            gap: 6px;
            margin-bottom: 12px;
        }

        .evento-meta-item {
            font-size: 0.9rem;
            color: #64748b;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .evento-meta-item i {
            color: var(--secondary);
            width: 16px;
            text-align: center;
        }

        .evento-description {
            font-size: 0.9rem;
            color: #64748b;
            line-height: 1.65;
            margin-bottom: 15px;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .evento-actions {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
            margin-top: 4px;
        }

        .evento-btn {
            display: inline-flex;
            align-items: center;
            gap: 7px;
            padding: 9px 20px;
            border-radius: 25px;
            font-size: 0.85rem;
            font-weight: 600;
            text-decoration: none;
            transition: all 0.25s;
            letter-spacing: 0.2px;
        }

        .evento-btn-primary {
            background: linear-gradient(135deg, var(--primary), #1e40af);
            color: white;
            box-shadow: 0 4px 12px rgba(30, 58, 138, 0.25);
        }

        .evento-btn-primary:hover {
            background: linear-gradient(135deg, #1e40af, var(--primary-dark));
            transform: translateY(-1px);
            box-shadow: 0 6px 18px rgba(30, 58, 138, 0.35);
        }

        .evento-btn-secondary {
            background: #f1f5f9;
            color: var(--primary);
            border: 1px solid #e2e8f0;
        }

        .evento-btn-secondary:hover {
            background: var(--secondary);
            color: white;
            border-color: var(--secondary);
            transform: translateY(-1px);
        }

        .eventos-divider {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            margin: 50px 0 40px;
            color: #cbd5e1;
            font-size: 0.85rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .eventos-divider::before,
        .eventos-divider::after {
            content: '';
            flex: 1;
            height: 1px;
            background: linear-gradient(90deg, transparent, #e2e8f0, transparent);
        }

        .eventos-empty {
            text-align: center;
            padding: 5rem 2rem;
            color: #94a3b8;
        }

        .eventos-empty i {
            font-size: 3rem;
            margin-bottom: 1.5rem;
            display: block;
            color: #cbd5e1;
        }

        .eventos-empty h3 {
            font-size: 1.2rem;
            font-weight: 600;
            color: #64748b;
            margin-bottom: 0.5rem;
        }

        .eventos-count-badge {
            display: inline-flex;
            align-items: center;
            background: #f1f5f9;
            color: #64748b;
            padding: 3px 10px;
            border-radius: 12px;
            font-size: 0.78rem;
            font-weight: 600;
            margin-left: 8px;
        }

        @media (max-width: 768px) {
            .hero-eventos {
                padding: 40px 15px;
                min-height: 35vh;
            }

            .eventos-container {
                padding: 25px 15px;
            }
        }

        /* Events Carousel / Banner */
        .events-carousel-section {
            background:
                radial-gradient(ellipse at 15% 50%, rgba(255,140,0,0.18) 0%, transparent 55%),
                radial-gradient(ellipse at 85% 20%, rgba(59,130,246,0.25) 0%, transparent 50%),
                linear-gradient(135deg, #0a1628 0%, #0f2060 35%, #1e3a8a 65%, #1a3580 100%);
            padding: 30px 0;
            margin-bottom: 10px;
            position: relative;
            overflow: hidden;
        }

        .events-carousel-bg {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background-size: cover;
            background-position: center;
            filter: blur(3px) brightness(0.3);
            transform: scale(1.05);
            pointer-events: none;
            z-index: 0;
        }

        .events-carousel-section > *:not(.events-carousel-bg) {
            position: relative;
            z-index: 1;
        }

        .events-carousel-title {
            text-align: center;
            color: white;
            font-family: 'Raleway', sans-serif;
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 20px;
        }

        .events-carousel-track {
            display: flex;
            gap: 20px;
            overflow-x: auto;
            scroll-snap-type: x mandatory;
            padding: 0 40px 15px;
            scrollbar-width: thin;
            scrollbar-color: rgba(255,255,255,0.3) transparent;
        }

        .events-carousel-track::-webkit-scrollbar {
            height: 6px;
        }

        .events-carousel-track::-webkit-scrollbar-track {
            background: transparent;
        }

        .events-carousel-track::-webkit-scrollbar-thumb {
            background: rgba(255,255,255,0.3);
            border-radius: 3px;
        }

        .events-carousel-slide {
            flex: 0 0 auto;
            width: 300px;
            scroll-snap-align: start;
            border-radius: 15px;
            overflow: hidden;
            background: white;
            box-shadow: 0 8px 30px rgba(0,0,0,0.2);
            transition: transform 0.3s ease;
            text-decoration: none;
            display: block;
        }

        .events-carousel-slide:hover {
            transform: translateY(-5px) scale(1.02);
        }

        .events-carousel-slide img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            display: block;
        }

        .events-carousel-info {
            padding: 15px;
            text-align: center;
        }

        .events-carousel-info h4 {
            font-size: 0.95rem;
            font-weight: 700;
            color: var(--primary-dark);
            margin-bottom: 4px;
            font-family: 'Raleway', sans-serif;
        }

        .events-carousel-info p {
            font-size: 0.8rem;
            color: #64748b;
        }

        @media (max-width: 768px) {
            .events-carousel-track {
                padding: 0 20px 15px;
            }
            .events-carousel-slide {
                width: 260px;
            }
        }

        @media (prefers-reduced-motion: reduce) {
            .evento-card:hover {
                transform: none;
            }
        }
    </style>
  </Fragment>

    <!-- Hero Section -->
    <section class="hero-eventos">
        <div class="hero-content">
            <div class="hero-badge"><i class="fas fa-trophy"></i> <span data-es="Competencias Internacionales" data-en="International Competitions">Competencias Internacionales</span></div>
            <h1 data-es="Eventos FEMUNDO" data-en="FEMUNDO Events">Eventos FEMUNDO</h1>
            <p data-es="Calendario completo de torneos, campeonatos y competencias internacionales de dominó" data-en="Complete calendar of domino tournaments, championships and international competitions">Calendario completo de torneos, campeonatos y competencias internacionales de dominó</p>
        </div>
    </section>

    <!-- Upcoming Events Carousel Banner -->
    {upcomingEvents.length > 0 && (
        <section class="events-carousel-section">
            {carouselBg && <div class="events-carousel-bg" style={`background-image: url('${carouselBg}')`}></div>}
            <h3 class="events-carousel-title">
                <i class="fas fa-star" style="color: #ff8c00; margin-right: 8px;"></i>
                <span data-es="Eventos Destacados" data-en="Featured Events">Eventos Destacados</span>
            </h3>
            <div class="events-carousel-track">
                {upcomingEvents.map((evt: any) => (
                    <a href={`/eventos/${evt.slug}`} class="events-carousel-slide">
                        {(evt.flyer_image_url || evt.image_url || evt.cover_image_url) ? (
                            <img src={evt.flyer_image_url || evt.image_url || evt.cover_image_url} alt={evt.name_es} loading="lazy" />
                        ) : (
                            <div style="width:100%;height:200px;background:linear-gradient(135deg,#1e3a8a,#ff8c00);display:flex;align-items:center;justify-content:center;">
                                <i class="fas fa-trophy" style="font-size:3rem;color:rgba(255,255,255,0.3);"></i>
                            </div>
                        )}
                        <div class="events-carousel-info">
                            <h4 data-es={evt.name_es} data-en={evt.name_en || evt.name_es}>{evt.name_es}</h4>
                            {evt.location && <p><i class="fas fa-map-marker-alt" style="margin-right:4px;color:#ff8c00;"></i>{evt.location}</p>}
                            {evt.start_date && <p><i class="fas fa-calendar" style="margin-right:4px;color:#ff8c00;"></i><span data-es={formatDate(evt.start_date)} data-en={formatDateEn(evt.start_date)}>{formatDate(evt.start_date)}</span></p>}
                        </div>
                    </a>
                ))}
            </div>
        </section>
    )}

    <div class="eventos-container">
        <!-- Upcoming Events -->
        {upcomingEvents.length > 0 && (
            <div>
                <h2 class="eventos-section-title">
                    <i class="fas fa-calendar-star"></i>
                    <span data-es="Próximos Eventos" data-en="Upcoming Events">Próximos Eventos</span>
                    <span class="eventos-count-badge">{upcomingEvents.length}</span>
                </h2>
                <div class="eventos-grid">
                    {upcomingEvents.map((evt: any) => (
                        <a href={`/eventos/${evt.slug}`} class="evento-card">
                            <div class="evento-card-image-wrap">
                                {(evt.flyer_image_url || evt.image_url || evt.cover_image_url) ? (
                                    <img src={evt.flyer_image_url || evt.image_url || evt.cover_image_url} alt={evt.name_es} class="evento-card-image" loading="lazy" />
                                ) : (
                                    <div class="evento-card-no-image">
                                        <i class="fas fa-calendar-star"></i>
                                    </div>
                                )}
                            </div>
                            <div class="evento-card-body">
                                <span class={`evento-status status-${evt.status}`}>
                                    {evt.status === 'upcoming' ? '🗓 Próximo' : '🔴 En curso'}
                                </span>
                                <h3 class="evento-card-title" data-es={evt.name_es} data-en={evt.name_en || evt.name_es}>{evt.name_es}</h3>
                                <div class="evento-meta">
                                    {evt.location && (
                                        <div class="evento-meta-item">
                                            <i class="fas fa-map-marker-alt"></i>
                                            <span>{evt.location}{evt.country ? `, ${evt.country}` : ''}</span>
                                        </div>
                                    )}
                                    {evt.start_date && (
                                        <div class="evento-meta-item">
                                            <i class="fas fa-calendar-alt"></i>
                                            <span data-es={formatDate(evt.start_date)} data-en={formatDateEn(evt.start_date)}>{formatDate(evt.start_date)}</span>
                                        </div>
                                    )}
                                </div>
                                {evt.description_es && (
                                    <p class="evento-description" data-es={evt.description_es} data-en={evt.description_en || evt.description_es}>{evt.description_es}</p>
                                )}
                                <div class="evento-actions">
                                    <span class="evento-btn evento-btn-primary">
                                        <i class="fas fa-info-circle"></i> <span data-es="Ver detalles" data-en="View details">Ver detalles</span>
                                    </span>
                                    {evt.registration_url && (
                                        <span class="evento-btn evento-btn-secondary" onclick={`event.preventDefault(); window.location.href='${evt.registration_url}'`}>
                                            <i class="fas fa-user-plus"></i> <span data-es="Inscribirse" data-en="Register">Inscribirse</span>
                                        </span>
                                    )}
                                </div>
                            </div>
                        </a>
                    ))}
                </div>
            </div>
        )}

        {upcomingEvents.length > 0 && pastEvents.length > 0 && (
            <div class="eventos-divider" role="separator">
                <span data-es="Historial" data-en="History">Historial</span>
            </div>
        )}

        <!-- Past Events -->
        {pastEvents.length > 0 && (
            <div>
                <h2 class="eventos-section-title">
                    <i class="fas fa-trophy"></i>
                    <span data-es="Eventos Anteriores" data-en="Past Events">Eventos Anteriores</span>
                    <span class="eventos-count-badge">{pastEvents.length}</span>
                </h2>
                <div class="eventos-grid">
                    {pastEvents.map((evt: any) => (
                        <a href={`/eventos/${evt.slug}`} class="evento-card">
                            <div class="evento-card-image-wrap">
                                {(evt.flyer_image_url || evt.image_url || evt.cover_image_url) ? (
                                    <img src={evt.flyer_image_url || evt.image_url || evt.cover_image_url} alt={evt.name_es} class="evento-card-image" loading="lazy" />
                                ) : (
                                    <div class="evento-card-no-image">
                                        <i class="fas fa-trophy"></i>
                                    </div>
                                )}
                            </div>
                            <div class="evento-card-body">
                                <span class="evento-status status-completed" data-es="✓ Completado" data-en="✓ Completed">✓ Completado</span>
                                <h3 class="evento-card-title" data-es={evt.name_es} data-en={evt.name_en || evt.name_es}>{evt.name_es}</h3>
                                <div class="evento-meta">
                                    {evt.location && (
                                        <div class="evento-meta-item">
                                            <i class="fas fa-map-marker-alt"></i>
                                            <span>{evt.location}{evt.country ? `, ${evt.country}` : ''}</span>
                                        </div>
                                    )}
                                    {evt.start_date && (
                                        <div class="evento-meta-item">
                                            <i class="fas fa-calendar-alt"></i>
                                            <span data-es={formatDate(evt.start_date)} data-en={formatDateEn(evt.start_date)}>{formatDate(evt.start_date)}</span>
                                        </div>
                                    )}
                                </div>
                                {evt.description_es && (
                                    <p class="evento-description" data-es={evt.description_es} data-en={evt.description_en || evt.description_es}>{evt.description_es}</p>
                                )}
                                <div class="evento-actions">
                                    <span class="evento-btn evento-btn-primary">
                                        <i class="fas fa-info-circle"></i> <span data-es="Ver detalles" data-en="View details">Ver detalles</span>
                                    </span>
                                    {evt.gallery_url && (
                                        <span class="evento-btn evento-btn-secondary" onclick={`event.preventDefault(); window.location.href='${evt.gallery_url}'`}>
                                            <i class="fas fa-images"></i> <span data-es="Ver galería" data-en="View gallery">Ver galería</span>
                                        </span>
                                    )}
                                </div>
                            </div>
                        </a>
                    ))}
                </div>
            </div>
        )}

        {upcomingEvents.length === 0 && pastEvents.length === 0 && (
            <div class="eventos-empty">
                <i class="fas fa-calendar-xmark"></i>
                <h3 data-es="No hay eventos disponibles" data-en="No events available">No hay eventos disponibles</h3>
                <p data-es="Pronto publicaremos información sobre próximos eventos" data-en="We will soon publish information about upcoming events">Pronto publicaremos información sobre próximos eventos</p>
            </div>
        )}
    </div>

  <Fragment slot="scripts">
    <script>
        if ('IntersectionObserver' in window) {
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

            document.querySelectorAll('.evento-card').forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                card.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
                observer.observe(card);
            });
        }
    </script>
  </Fragment>
</MainLayout>
